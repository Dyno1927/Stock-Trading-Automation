# Releases

Record of tagged releases. Each entry corresponds to a `release/vX.Y.Z` branch
merged into `main` and tagged. See `CHANGELOG.md` for detailed change lists.

<!-- Add an entry here once a release branch is merged into main and tagged. -->
